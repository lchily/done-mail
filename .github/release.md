# 1.0.1 更新说明

- 修正邮件策略条件编辑交互，布尔条件现在使用“是 / 否”选择器，避免手动输入导致的匹配值不规范。
- 优化“有附件”和“转发接收”条件的保存逻辑，统一使用等于判断并自动规范化为 `true` / `false`。
- 调整新建策略默认行为，默认使用“任一条件命中”并在命中后停止继续匹配，减少误匹配风险。
- 新增 GitHub Actions 发布流程，推送 `main` 后会执行检查，并基于 `package.json` 版本创建标签和 GitHub Release。
